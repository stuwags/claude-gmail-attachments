node_modules/
dist/
dist-ssr/
# Screenshot harness output — regenerate with `npm run shots`.
shots/
# Scratch probes written while investigating a frame.
tools/.tmp/
# Capacitor native projects are scaffolded on the developer's Mac.
ios/
android/
*.local
.DS_Store

# Throwaway captures from tools/playtest runs.
playtest/
dist-standalone/
dist-artifact/

# Output of `npm run ladder`.
ladder-result.txt
